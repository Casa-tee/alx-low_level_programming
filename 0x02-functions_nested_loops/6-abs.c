#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @t: the character
 * Return: Absolute value
*/
int _abs(int t)
{
	int t;

	if (t >= 0)
		return (t);
	else
		return (-t);
}
